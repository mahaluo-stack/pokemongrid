import styled from 'styled-components';

export const PageContainer = {
    Body: styled.div`
        width: 100%;
        max-width: 100vw;
        height: auto;
        min-height: 100vh;
        margin: auto;
    `
}